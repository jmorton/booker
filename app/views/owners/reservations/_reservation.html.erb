<div>
  <h4>
    <%= link_to reservation.label, owner_reservation_path(reservation) %>
    for
    <%= link_to reservation.unit.label, owner_unit_path(reservation.unit) %>
  </h4>
  <time data-attr="start_at"><%= l(reservation.start_at) %></time> &mdash;
  <time data-attr="end_at"><%= l(reservation.end_at) %></time>
</div>
