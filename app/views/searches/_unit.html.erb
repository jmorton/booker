<li class="unit">
  <h4><%= link_to unit.label, guest_unit_path(unit) %></h4>
  <div>Price, Features, Rating</div>
  <%= link_to "reserve", new_guest_reservation_path(reservation: {
        unit_id: unit, start_at: @search.start_at.to_date, end_at: @search.end_at.to_date
      }) %>
</li>
